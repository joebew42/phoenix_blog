<h1 class="ui header">List of all users</h1>

<%= link "New User", to: user_path(@conn, :new), class: "ui primary button" %>

<table class="ui celled table">
  <thead>
    <tr>
      <th>Uuid</th>
      <th>Username</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <%= for user <- @users do %>
    <tr>
      <td><%= user.uuid %></td>
      <td><%= user.username %></td>
      <td class="right aligned">
        <%= link "Show", to: user_path(@conn, :show, user), class: "ui button" %>
        <%= link "Edit", to: user_path(@conn, :edit, user), class: "ui button" %>
        <%= link "Delete", to: user_path(@conn, :delete, user), method: :delete, data: [confirm: "Are you sure?"], class: "ui negative button" %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
